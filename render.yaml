services:
  - type: web
    name: pocketbase
    env: static
    plan: free
    buildCommand: ""
    startCommand: ./pocketbase serve --http 0.0.0.0:$PORT
    envVars:
      - key: PB_URL
        value: "https://healthcopilot-pocketbase.onrender.com"
